dtbo-$(CONFIG_ARCH_CAPE) += cape-audio.dtbo
dtbo-$(CONFIG_ARCH_CAPE) += oplus/ovaltine-21841-audio-cape-overlay.dtbo

 always-y    := $(dtb-y) $(dtbo-y)
 subdir-y    := $(dts-dirs)
 clean-files    := *.dtb *.dtbo
